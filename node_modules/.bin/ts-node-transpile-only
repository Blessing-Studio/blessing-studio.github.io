#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../@commitlint/load/node_modules/ts-node/dist/bin-transpile.js" "$@"
  ret=$?
else 
  node  "$basedir/../@commitlint/load/node_modules/ts-node/dist/bin-transpile.js" "$@"
  ret=$?
fi
exit $ret
